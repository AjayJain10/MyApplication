<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ajayj.myapplication">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_ajay_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_ajay_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".LoggedinActivity" />
        <activity android:name=".ForgotpwdActivity" />
        <activity android:name=".StartActivity" />
        <activity android:name=".RloginActivity" />
        <activity android:name=".CheckEmailActivity" />
        <activity android:name=".RloggedinActivity" />
        <activity android:name=".RhomeActivity" />
        <activity android:name=".RmainActivity" />
        <activity android:name=".RcheckEmailActivity" />
        <activity android:name=".RforgotpwdActivity" />
        <activity android:name=".DemoActivity" />
        <activity android:name=".ButterknifeDemoActivity" />
        <activity android:name=".ImageButtonDemoActivity" />
        <activity android:name=".Toggle_Activity" />
        <activity android:name=".LoggedinAssignmentActivity" />
        <activity android:name=".ForgotpwdAssignmentActivity" />
        <activity android:name=".OtpSentActivity" />
        <activity android:name=".RadioandCheckboxDemoActivity" />
        <activity
            android:name=".BLoginAssignmentActivity"
            android:label="@string/title_activity_blogin_assignment"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".BSignupActivity"
            android:label="@string/title_activity_bsignup"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".SignedupActivity" />
        <activity
            android:name=".BDateTimeActivity"
            android:label="@string/title_activity_bdate_time"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".RecyclerActivity" />
        <activity
            android:name=".AnimationActivity"
            android:label="@string/title_activity_animation"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".FragmentsActivity" />
        <activity
            android:name=".LauncherActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".NavigationDrawerActivity"
            android:label="@string/title_activity_navigation_drawer"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".PhoneActivity" />
        <activity android:name=".BarCodeActivity" />
        <activity android:name=".WebViewActivity" />
        <activity android:name=".BluetoothActivity" />
        <activity android:name=".LinkingActivity" />
        <activity android:name=".WifiActivity" />
        <activity android:name=".SensorActivity" />
        <activity android:name=".RadioBtnLoginSignupActivity" />
        <activity
            android:name=".TabsActivity"
            android:label="@string/title_activity_tabs"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>